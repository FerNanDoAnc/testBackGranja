# Impresionante creación de proyectos con TypeORM

Recordar

1. Ejecute el comando `npm i`
2. Configure los ajustes de la base de datos dentro del archivo `ormconfig.json`
3. Ejecute el comando `npm start`